<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.FirstAppActivity"
        tools:targetApi="31">
        <activity
            android:name=".buscaTuPerro.ui.activity.ApiDogActivity"
            android:exported="false" />
        <activity
            android:name=".superHeroApp.DetailSuperHeroActivity"
            android:exported="false" />
        <activity
            android:name=".superHeroApp.SuperHeroListActivity"
            android:exported="false" />
        <activity
            android:name=".IMC.ResultIMCActivity"
            android:exported="false" />
        <activity
            android:name=".IMC.IMCActivity"
            android:exported="false" />
        <activity
            android:name=".menuActivity.MenuActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".saludoapp.ResultActivity"
            android:exported="false" />
        <activity
            android:name=".saludoapp.FirstAppActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>